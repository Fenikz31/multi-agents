//! TUI navigation system placeholder
// Will orchestrate view switching and shortcuts in later tasks.

#[allow(dead_code)]
pub enum ViewId {
    ProjectSelect,
    Kanban,
    Sessions,
    Detail,
    Help,
}


